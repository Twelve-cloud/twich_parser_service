version: '3.7'

x-env: &default-env
  env_file:
    - ./tlparser/env/tests/.env.tests.web

services:
  tests:
    <<: *default-env
    build:
      context: ./tlparser
      dockerfile: ./docker/Dockerfile.pytest.tests
    entrypoint:
      - ./entrypoint-pytest.tests.sh
    networks:
      back-tests:
        ipv4_address: 182.168.10.4
    links:
      - redis
      - mongo
    depends_on:
      redis:
        condition: service_healthy
      mongo:
        condition: service_healthy
    restart: no